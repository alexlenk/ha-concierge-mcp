pytest-homeassistant-custom-component==0.13.348
mcp>=1.9.0,<2.0.0
PyJWT[crypto]>=2.0.0

# Pulled in only so tests can set up the official mcp_server integration
# alongside this one, to prove FR-8 (no cross-integration interference).
# Versions must track homeassistant.components.conversation's manifest.json
# pins for the homeassistant version pytest-homeassistant-custom-component
# above resolves to — check that manifest during each upstream sync review
# (design document, section 12.1) rather than assuming these stay correct.
# Pinned to HA 2026.7.4's conversation/manifest.json (0.13.348 -> homeassistant==2026.7.4).
aiohttp_sse
hassil==3.8.0
home-assistant-intents==2026.6.24
